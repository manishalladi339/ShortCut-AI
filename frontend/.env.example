# Expo development / native devices: use the reachable API origin.
EXPO_PUBLIC_BACKEND_URL=http://localhost:8001
# Legacy Emergent OAuth remains opt-in; email/password works independently.
EXPO_PUBLIC_GOOGLE_AUTH_ENABLED=false
